.page__content.content
  .container.content__container
    section.content__recent-posts.recent-posts
      h3.content__title.section-title
        span.section-title__marker Recently
        | Posted
      if indexPageVariant === 1
        ul.recent-posts__list
          each item in pugData.recent_posts.slice(0, 3)
            li.recent-posts__item
              article.small-post.small-post--medium
                img(src= item.image width="401" height="229", alt= item.title).small-post__image
                .small-post__info
                  h4.small-post__title
                    a(href= item.link).small-post__link= item.title
                  a(href= item.categorie_url).small-post__badge= item.categorie
                  .small-post__description
                    .small-post__meta
                      a(href= item.author_link).small-post__writer-link
                        img(src= item.author_image, width="18" height="18" alt="Author Post - " + item.author_name).small-post__photo-author
                        p.small-post__author-name= item.author_name
                      time(datetime= item.post_datetime).small-post__date= item.post_date
                      p.small-post__reading-time= item.reading_time
                    p.small-post__post-text= item.post_text
      if indexPageVariant === 2
        ul.recent-posts__list
          each item in pugData.recent_posts.slice(0, 2)
            li.recent-posts__item
              article.small-post.small-post--medium-no-image
                .small-post__info
                  h4.small-post__title
                    a(href= item.link).small-post__link= item.title
                  a(href= item.categorie_url).small-post__badge= item.categorie
                  .small-post__description
                    .small-post__meta
                      a(href= item.author_link).small-post__writer-link
                        img(src= item.author_image, width="18" height="18" alt="Author Post - " + item.author_name).small-post__photo-author
                        p.small-post__author-name= item.author_name
                      time(datetime= item.post_datetime).small-post__date= item.post_date
                      p.small-post__reading-time= item.reading_time
                    p.small-post__post-text= item.post_text
      if indexPageVariant != 3
        include _invitation.pug
      if indexPageVariant === 1
        ul.recent-posts__list
          each item in pugData.recent_posts.slice(3)
            li.recent-posts__item
              article.small-post.small-post--medium
                img(src= item.image width="401" height="229", alt= item.title).small-post__image
                .small-post__info
                  h4.small-post__title
                    a(href= item.link).small-post__link= item.title
                  a(href= item.categorie_url).small-post__badge= item.categorie
                  .small-post__description
                    .small-post__meta
                      a(href= item.author_link).small-post__writer-link
                        img(src= item.author_image, width="18" height="18" alt="Author Post - " + item.author_name).small-post__photo-author
                        p.small-post__author-name= item.author_name
                      time(datetime= item.post_datetime).small-post__date= item.post_date
                      p.small-post__reading-time= item.reading_time
                    p.small-post__post-text= item.post_text
      if indexPageVariant === 2
        ul.recent-posts__list
          each item in pugData.recent_posts.slice(2)
            li.recent-posts__item
              article.small-post.small-post--medium-no-image
                .small-post__info
                  h4.small-post__title
                    a(href= item.link).small-post__link= item.title
                  a(href= item.categorie_url).small-post__badge= item.categorie
                  .small-post__description
                    .small-post__meta
                      a(href= item.author_link).small-post__writer-link
                        img(src= item.author_image, width="18" height="18" alt="Author Post - " + item.author_name).small-post__photo-author
                        p.small-post__author-name= item.author_name
                      time(datetime= item.post_datetime).small-post__date= item.post_date
                      p.small-post__reading-time= item.reading_time
                    p.small-post__post-text= item.post_text
      if indexPageVariant === 3
        ul.recent-posts__list.recent-posts__list--narrow
          each item in pugData.recent_posts.slice(0,-1)
            li.recent-posts__item
              article.small-post.small-post--narrow
                img(src= item.image width="401" height="229", alt= item.title).small-post__image
                .small-post__info
                  h4.small-post__title
                    a(href= item.link).small-post__link= item.title
                  a(href= item.categorie_url).small-post__badge= item.categorie
                  .small-post__description
                    .small-post__meta
                      a(href= item.author_link).small-post__writer-link
                        img(src= item.author_image, width="18" height="18" alt="Author Post - " + item.author_name).small-post__photo-author
                        p.small-post__author-name= item.author_name
                      time(datetime= item.post_datetime).small-post__date= item.post_date
                      p.small-post__reading-time= item.reading_time
                    p.small-post__post-text= item.post_text
      .pagination
        a(title="Previous page").pagination__control.pagination__control--prev
          svg(width="11" height="8" class="pagination__icon" aria-hidden="true")
            use(xlink:href="img/sprite.svg#pagination-arrow-left")
          span Prev
        ul.pagination__list
          li.pagination__item
            a(title="Page 1").pagination__link 1
          li.pagination__item
            a(href="#" title="Page 2").pagination__link 2
          li.pagination__item
            a(href="#" title="Page 3").pagination__link 3
        a(href="#" title="Next page").pagination__control.pagination__control--next
          svg(width="11" height="8" class="pagination__icon" aria-hidden="true")
            use(xlink:href="img/sprite.svg#pagination-arrow-right")
          span Next
    aside.content__main-sidebar.main-sidebar
      include _sidebar-top-authors.pug
      include _sidebar-ad.pug
      include _sidebar-categories.pug
      include _sidebar-statistics.pug
      include _sidebar-instafeed.pug
      include _sidebar-tags.pug

